# 验证记录

版本：v0.3.0。验证日期：2026-09-16。新增 uni-app 小程序、独立令牌认证、微信绑定接口和 H5 手机预览；原有网页版和学习数据继续沿用。

## 自动化检查

- `npm run build`：TypeScript 类型检查与 Vite 生产构建通过。
- `npm test`：12 项测试通过，覆盖日期与统计工具、计时按账户保存与旧版迁移、切换账户后丢弃旧请求结果、请求的账户和 CSRF 校验信息、会话过期处理。
- `.venv/Scripts/python.exe -m pytest -q`：85 项测试通过，包含原有 57 项回归与新增 28 项小程序认证测试。
- `miniapp/` 中的 `npm test`：12 项测试通过，覆盖切后台和暂停后的计时恢复、24 小时上限、按服务与账户保存、令牌更新、存储失败提示、请求归属和旧响应丢弃。
- `miniapp/` 中的 `npm run build:mp-weixin`、`npm run build:h5`：类型检查和两种生产构建通过。微信输出包含 11 个页面、5 个原生 tab、WXML / WXSS / JS 与配置文件。
- 新增接口测试覆盖网页 Cookie 与小程序 Bearer 不可互换、无错误回退、账户和写入保护、注册与登录节流、会话过期和退出、改密撤销两端会话、微信绑定凭证的时效和一次性消费、绑定冲突、解绑撤销、并发与微信网络失败。微信请求释放 SQLite 写锁后才调用上游接口。
- 恢复测试覆盖完整往返、合并重复与冲突、非法数据与断裂引用、预览过期与数据变化、恢复前备份、磁盘和数据库失败回滚、重复及并发确认、备份完整性校验、跨账户访问拒绝。
- 迁移测试使用独立 v0.1 数据库，验证所有业务记录与复习历史保留、升级前快照可下载、重启持久化、迁移中途失败时连同表结构一起回滚。
- `npm run check:release`：当前未配置真实 AppID，且使用本机 HTTP 地址，命令按预期以非零状态退出并说明两项缺失。没有把本地配置当作可以发布的配置。

共 109 项自动化测试通过。输出保存在 `.test-results/pytest-v03.txt`、`vitest-web-v03.txt`、`vitest-mini-v03.txt`；三端构建日志为同目录的 `build-web-v03.txt`、`build-mp-v03.txt`、`build-h5-v03.txt`。

Python 测试依赖目前输出两条上游弃用提示（Starlette 的 httpx 适配、AnyIO 的 BlockingPortal 别名），不影响以上测试结果。

小程序依赖审计保存在 `.test-results/miniapp-audit-v03.json`。兼容升级后报告 0 项严重、0 项高危、7 项中危、13 项低危。剩余提示涉及 uni-app 的 CSS / 图像处理依赖链和 Vitest 开发依赖，尚未全部消除；本版测试使用一次性 `vitest run`，不启动对外测试服务。未使用会跨版本替换 uni-app 的强制自动修复。

## 浏览器验收

使用独立 SQLite 验收数据库，不向交付数据库写入测试记录。

v0.3 小程序 H5 生产构建验收，服务为 `127.0.0.1:8767`，视口为 390 × 844：

- 使用合成验收账号登录，成功读取此前保存的 45 分钟、20 题 / 16 题正确的学习记录，首页显示 80% 正确率与 1 天打卡。
- 新建 30 分钟申论计划，进入专注页时正确带入申论科目。计时暂停在 1 分 32 秒后刷新，保持暂停与原时长；继续后可结束并进入记录表单。
- 申论表单隐藏客观题题量，显示作答与反馈。核对为 30 分钟并保存后，计时清空、计划完成，总时长增至 75 分钟，正确率仍为 80%。同时修正保存后返回首页，以及暂停、编辑记录时的提示文字。
- 新建当天到期的复习卡片，先回忆再揭示笔记；提交实际 8 分钟与“掌握”反馈后，到期数量归零，已复习次数为 1，下次日期为 9 月 19 日。
- 统计页显示总计 83 分钟、3 次学习、1 天打卡、80% 正确率，言语 53 分钟、申论 30 分钟。7 / 30 天切换、日历当天明细及未来 7 天复习分布与记录一致。
- 按“申论概括”筛选仅保留 1 条记录；修改笔记后仍为 30 分钟，没有新增重复记录。
- 在 `localhost:8767` 的网页版登录同一合成账号，显示小程序保存的三条记录、复习反馈及更新后的申论笔记。
- 本轮小程序与网页验收均未捕获控制台错误或警告。小屏下检查首页、专注、记录表单、复习和统计日历，内容正常排列，底部导航保持可用。
- 交付服务 `127.0.0.1:8765/mini/` 正常打开初始化账号页面，保留给使用者创建自己的账号。

此前 v0.2 浏览器验收记录，相关自动化测试已在本轮回归：

- 上传合成 JSON 备份，预览显示 7 个相同科目、各 1 条新增计划 / 学习记录 / 复习卡片；不同备考目标按合并规则保留当前值。
- 确认合并后，首页显示 45 分钟、80% 正确率、1 天打卡、计划完成 1 / 1，且自动生成恢复前备份。
- 从自动备份历史预览恢复，切换完整替换后，必须勾选确认才能执行；恢复完成后保留新的恢复前备份。
- 390 × 844 手机视口下检查恢复弹窗，内容可纵向滚动，对照表在自身区域横向滚动；未保存专注计时会阻止恢复并提供处理入口。
- 启用测试账户后，原匿名页面要求登录；登录原账户可找回该浏览器保存的计时。
- 两个账户分别显示 25 / 35 分钟的专属记录，计时没有跨账户展示；一个页面退出后，另一个页面同步隐藏原账户内容，重新登录后切换到对应记录。
- CSV 改为服务端直接下载，在已登录页面成功触发浏览器下载事件；服务端仍校验链接所属账户，防止旧页面下载另一个账户的数据。

此前原有学习流程验收记录：

- 新建资料分析计划，完成时自动带入科目、内容与预计用时。
- 保存实际 45 分钟、20 道题、16 道正确的记录后，页面显示 45 分钟、80% 正确率、1 天打卡、计划完成 1 / 1。
- 勾选同时加入复习后，生成下一日复习卡并保留学习笔记。
- 调整卡片到期日，先回忆后展开笔记；提交 8 分钟“已经掌握”后，下一次复习安排在 3 天后，新增一次学习记录与复习历史。
- 启动计时，刷新后保留运行状态；结束时带入日期、科目、内容与时长，保存后计时清空。
- 手机宽度下检查首页和记录弹窗；切换到申论时隐藏客观题题量，显示作答反馈字段。保存 30 分钟申论记录后，总时长增加，客观题正确率保持 80%。
- 390 × 844 视口下逐页验证计划、复习、记录和复盘导航，没有横向溢出；移动端计划删除入口可见。
- 浏览器验收未捕获到控制台错误或警告。

## 验证边界

交付服务运行在 `http://127.0.0.1:8765`，健康接口返回 v0.3.0。`/mini/` 返回 200，并正确引用 `/mini/assets/` 下的生产资源。原有网页版继续在根路径使用。

升级前通过 SQLite 在线备份保存到 `data/pre-v03/20260916T032622426336Z/zhian.db`，同目录保存各表数量与内容散列。重启后，账户、目标、科目、计划、学习记录、复习卡片、复习历史、网页会话、备份登记和导入任务共 10 张原表的全部内容散列一致；数据库完整性与外键检查均通过。新增三张小程序表成功创建，没有写入验收账号或学习记录。检查结果保存在 `.test-results/delivery-v03-verified.json`。此前 `data/pre-v02/` 与自动升级备份也保留。

已验证 Windows + SQLite、uni-app 微信包编译、H5 页面流程与两端共用数据。微信网络请求在自动化测试中使用模拟响应；没有真实 AppID / AppSecret，没有在微信开发者工具或真机上完成微信登录、绑定与发布验收。当前微信包为游客 AppID，不能据此声称已经上线微信。

提供了 Docker Compose / Caddy HTTPS 配置与 PostgreSQL 连接说明；当前电脑没有 Docker 运行环境，容器构建、真实证书签发、PostgreSQL 实例、手机跨网络访问尚未实机验证，也未发布公网网站。后台订阅提醒、离线提交队列与密码自助找回不在本版实现范围。

## 微信发布资料准备

日期：2026-09-21。发布资料位于 `publish/wechat/`，已包含：

- 主图标和浅色图标 SVG，以及 1024、512、144、108 像素 PNG 导出；图标尺寸和 PNG 文件头已校验。
- “知岸学习”首选名称、三个备选名称、短简介、长介绍、审核备注和客服文案。
- 工具类目建议、备案材料清单、主体信息采集表和发布前逐项检查表。
- 隐私政策 Markdown 与可直接部署的 HTML 页面、微信隐私保护指引填写表和常见审核问答。
- 六张基于隔离测试数据的 390 × 844 审核截图：今日、计划、复习、统计、我的和登录页。
- 36 个文件的 SHA-256 校验清单和 `publish/wechat-export/zhian-wechat-materials.zip` 打包副本；另有服务器部署、微信后台配置和开发者工具上传的完整步骤文档。

资料中的主体名称、联系邮箱、正式域名、AppID、备案号和服务类目仍保留待填写状态。没有把真实密码、AppSecret、手机号或备案信息写入资料包。正式提交前仍需要在微信后台核对名称可用性、当期类目要求，并补齐应用内隐私同意、账号注销或数据删除入口。
